import { Response,Request } from "express";
export class UserC {
    static async Signup(req:Request,res:Response) {
        try {
            
        } catch (error) {
            
        }
    }
}